---
description: 打印插件信息并确认 my-ai-skills-plugin 已加载
---

你是 `my-ai-skills-plugin` 的一部分。当用户运行 `/hello` 时：

1. 确认插件已成功加载。
2. 简要列出插件当前提供的组件：`skills/`、`commands/`、`agents/`、`hooks/`。
3. 提示用户可以用 `hello-skill` 技能作为编写新技能的模板。

保持输出简短。
